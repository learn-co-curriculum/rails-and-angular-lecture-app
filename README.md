# README

To get this application running correctly

run:

```
bundle install
```
followed by:
```
rake db:migrate
```
lastly:
```
rake bower:install
```

To run the app in the browser. Just run
```
rails server
```
